# Normalize to LF in the repository; check out native on Windows.
* text=auto

# Batch launchers stay CRLF on disk: some cmd.exe constructs are sensitive to it.
*.cmd text eol=crlf

*.ico binary
*.png binary
